{
  "name": "shopline-cart-notifier",
  "version": "0.1.0",
  "private": true,
  "description": "Embedded SHOPLINE app that records add-to-cart events and shows them in an admin dashboard.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "render-start": "HOST=0.0.0.0 node src/server.js"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0"
  }
}
